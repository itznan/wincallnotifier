name: Release Pipeline

on:
  push:
    branches:
      - main
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      tag_name:
        description: 'Release version tag (e.g., v0.1.0)'
        required: false
        type: string
        default: ''
      draft:
        description: 'Create as a draft release'
        required: false
        type: boolean
        default: false
      prerelease:
        description: 'Mark as a pre-release'
        required: false
        type: boolean
        default: false

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: write

jobs:
  build:
    name: Build and Package (Windows x86_64)
    runs-on: windows-latest

    steps:
      - name: Checkout Source Code
        uses: actions/checkout@v4

      - name: Initialize Rust Toolchain
        uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          toolchain: stable
          target: x86_64-pc-windows-msvc
          cache: true

      - name: Compile Release Binary
        run: cargo build --release --locked --target x86_64-pc-windows-msvc

      - name: Configure Inno Setup Compiler
        shell: powershell
        run: |
          $innoPath = "C:\Program Files (x86)\Inno Setup 6"
          if (Test-Path "$innoPath\ISCC.exe") {
            echo "$innoPath" | Out-File -FilePath $env:GITHUB_PATH -Encoding utf8 -Append
          } else {
            choco install innosetup --no-progress
            echo "C:\Program Files (x86)\Inno Setup 6" | Out-File -FilePath $env:GITHUB_PATH -Encoding utf8 -Append
          }

      - name: Resolve Release Version
        id: version_resolver
        shell: powershell
        run: |
          if ("${{ github.event_name }}" -eq "workflow_dispatch" -and "${{ inputs.tag_name }}" -ne "") {
            $tag = "${{ inputs.tag_name }}"
          } elseif ($env:GITHUB_REF -like "refs/tags/*") {
            $tag = $env:GITHUB_REF -replace "refs/tags/", ""
          } else {
            $ver = (Select-String -Path "Cargo.toml" -Pattern '^version\s*=\s*"([^"]+)"').Matches.Groups[1].Value
            $tag = "v$ver"
          }
          $cleanVersion = $tag.TrimStart("v")
          Write-Host "Resolved release tag: $tag"
          Write-Host "Resolved semantic version: $cleanVersion"
          "RELEASE_TAG=$tag" >> $env:GITHUB_ENV
          "APP_VERSION=$cleanVersion" >> $env:GITHUB_ENV

      - name: Build Windows Installer Package
        shell: powershell
        run: |
          ISCC.exe /DMyAppVersion="${{ env.APP_VERSION }}" installer.iss

      - name: Stage Release Artifacts
        shell: powershell
        run: |
          New-Item -ItemType Directory -Path "release_assets" -Force | Out-Null
          New-Item -ItemType Directory -Path "dist_portable" -Force | Out-Null

          # Stage files for portable ZIP package
          Copy-Item "target\x86_64-pc-windows-msvc\release\wincallnotifier.exe" -Destination "dist_portable\"
          Copy-Item "README.md" -Destination "dist_portable\"

          # Package 1: Windows Setup Installer (.exe)
          if (Test-Path "dist-installer\WinCallNotifier-Installer.exe") {
            Copy-Item "dist-installer\WinCallNotifier-Installer.exe" -Destination "release_assets\WinCallNotifier-Installer.exe"
          }

          # Package 2: Portable Archive (.zip)
          Compress-Archive -Path "dist_portable\*" -DestinationPath "release_assets\WinCallNotifier-Portable.zip" -Force

          # Package 3: Compute SHA256 Checksums
          Set-Location -Path "release_assets"
          $checksums = Get-ChildItem -File | ForEach-Object {
            $hash = (Get-FileHash -Algorithm SHA256 $_.FullName).Hash.ToLower()
            "$hash  $($_.Name)"
          }
          $checksums | Out-File -FilePath "SHA256SUMS.txt" -Encoding ascii
          Set-Location -Path ".."

          Get-ChildItem -Path "release_assets"

      - name: Upload Workflow Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: WinCallNotifier-Windows
          path: release_assets/*

      - name: Publish GitHub Release
        if: startsWith(github.ref, 'refs/tags/') || github.event_name == 'workflow_dispatch'
        uses: softprops/action-gh-release@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ env.RELEASE_TAG }}
          name: WinCallNotifier ${{ env.RELEASE_TAG }}
          draft: ${{ inputs.draft || false }}
          prerelease: ${{ inputs.prerelease || false }}
          generate_release_notes: true
          files: |
            release_assets/WinCallNotifier-Installer.exe
            release_assets/WinCallNotifier-Portable.zip
            release_assets/SHA256SUMS.txt
